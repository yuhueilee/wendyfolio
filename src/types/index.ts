export interface CardContent {
    img: string;
    title: string;
    description: string;
    links: Array<string>;
}

export interface ListContent {
    title: string;
    subTitle: string;
    duration: string;
    description: string;
}
